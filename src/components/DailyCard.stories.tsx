import DailyCard from "@/components/DailyCard";

const meta = {
  title: "Home/DailyCard",
  component: DailyCard,
  args: {
    userId: "storybook_user",
    defaultCategory: "shooting",
  },
};

export default meta;

export const Default = {};
